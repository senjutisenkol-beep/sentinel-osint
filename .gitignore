# AWS credentials - never commit these
.env
*.env
.aws/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/

# IDE
.vscode/
.idea/

# Test artifacts
*.log
*.tmp

# Credentials and secrets — never commit
*.txt
"pinecone details and AWS KB id.txt"

# Lambda build artifacts
*.zip
lambda_build/
lambda_build_kb/
package/

# Episodic run log — runtime data (append-only, regenerated per environment)
episodes/*.jsonl

# Local data / research files
Learnings/
Learnings.zip
agents/signal_monitor/response.json
agents/context_historian/graph/graph_auto_seeds.json
orchestration/cd

# Roadmap HTML files (local reference only)
*.html